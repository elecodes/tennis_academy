# DISABLED: Supabase REST API is no longer used.
# All reads go through direct pg8000 -> PostgreSQL.
name: Keep Supabase Alive (DISABLED)

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API (disabled)
        run: echo "Supabase REST no longer used — skipping."
