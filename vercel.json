{
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install -r backend/requirements.txt"
      }
    }
  ],
  "public": "public",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/app.py"
    }
  ]
}